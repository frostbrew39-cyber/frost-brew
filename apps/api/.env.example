JWT_SECRET=replace-with-secure-secret
# Render sets PORT automatically; locally omit or use 10000 to match POS dev defaults
PORT=10000
# Comma-separated Vercel (and preview) origins, no trailing slashes
# FRONTEND_URL=https://your-app.vercel.app,https://your-preview.vercel.app
FRONTEND_URL=
DATABASE_URL=
